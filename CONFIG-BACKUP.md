# 插件配置备份与恢复

所有有持久配置的插件都使用 OPNsense 的 `config.xml` 备份链路。原本位于
独立文件中的配置通过插件自己的 `OPNsense/<module>/backup` 节点保存；
运行时继续使用原有文件。系统完整 XML 导出、配置历史以及系统备份提供方
因此能携带这些配置。

| 插件 | 配置内容 | XML 节点 |
| --- | --- | --- |
| Staticarp | 设置、绑定条目、接口和 rc 启用状态 | `OPNsense/Staticarp/backup` |
| Lucky | rc 设置、实际配置目录中的账号、应用配置、证书和私钥，支持自定义目录 | `OPNsense/Lucky/backup` |
| DDNS-Go | 默认配置目录、rc 设置及 rc 指定的自定义配置文件，包括 DNS 提供商凭据 | `OPNsense/Ddnsgo/backup` |
| EasyTier | TOML 配置、网络密钥和 rc 启用状态 | `OPNsense/EasyTier/backup` |
| Ttyd | rc 设置、自定义终端命令、WebGUI 代理覆盖和旧版 TLS 证书、私钥 | `OPNsense/Ttyd/backup` |
| Sing-box | 配置、订阅 URL、环境文件、模板、rc 设置，以及配置明确引用的 TLS、SSH、规则、hosts、证书目录和 ACME 状态 | `OPNsense/SingBox/backup` |
| Mihomo | 设置、订阅快照、合并配置、明确引用的本地 provider 文件、代理选择及 DNS/TUN 归属记录 | `OPNsense/Mihomo/backup` |
| FRP | 客户端和服务端 TOML，包括认证凭据及各自的启用状态 | `OPNsense/Frp/backup` |
| Speedtest | 测速偏好，不包含测速历史 | `OPNsense/Speedtest/backup` |
| DDClient-opnwall | 直接使用原生 MVC 模型，包括 DNS 提供商凭据 | `OPNsense/DynDNS` |
| Unboundcustom | 直接使用原生 MVC 模型，包括自定义选项 | `OPNsense/unboundcustom` |
| 汉化工具 | 系统语言选择已经在系统配置内；下载的翻译文件属于可重新安装的软件内容 | `system/language` |
| Pftop | 没有持久配置；页面筛选参数和连接快照属于临时状态 | 无需配置镜像 |
| KazuhaRepo | 已安装软件包的恢复清单，保留有效的软件包名和版本 | `OPNsense/KazuhaRepo/backup` |

## 使用

1. 安装包含配置镜像支持的插件版本。安装时先还原已有 XML，再把现有文件
   接入系统备份；还原失败会保留原镜像并告警，镜像同步失败不会阻断安装。
2. 正常保存设置，确认没有同步警告，然后在 **系统 > 配置 > 备份** 导出完整配置。
3. 恢复完整系统配置，并按系统默认流程重新启动。插件在读取服务设置前
   还原文件；新设备还需要安装兼容的插件软件包。

新设备恢复后，可运行签名仓库引导脚本并传入 `--restore-plugins`：

```sh
fetch -o /tmp/kazuha-client.sh https://kkazuhak.github.io/OPNsense-repo/client.sh
sh /tmp/kazuha-client.sh --restore-plugins
```

脚本使用 XML 中的 KazuhaRepo 清单，从当前 OPNsense 系列的签名目录安装
兼容的新版本。会先检查全部软件包是否可用，再执行安装。未传入该选项时
只配置仓库。清单不受初始向导清空原生插件列表的影响；主动移除清单中的
软件包可运行 `kazuha.sh forget os-<name>`。

WebGUI 的保存和服务操作会同步配置镜像。六个使用共用组件的插件通过独立
后台任务每秒检查外部文件修改，Lucky 和 DDNS-Go 的内置页面修改也会自动进入镜像。
后台任务等待文件稳定约两秒；持续修改时，约 30 秒后尝试生成一次镜像。
同一插件的自动同步至少间隔 30 秒，这个间隔在后台进程重启后仍然有效；
没有近期同步时通常约 2–3 秒完成，近期同步后的改动最多等待约 30 秒。
页面保存和显式同步立即执行，并重置之后的自动同步等待时间。后台同步和
文件检查失败会限频写入系统日志；出现备份错误或警告时，应先解决错误再导出备份。
配置历史回退本身不会重新配置全部运行中的服务，应重新启动系统或受影响
的服务，使还原的配置生效。

Mihomo 恢复后保留服务启用选择，透明代理及其授权会关闭，需要在页面重新
确认后启用。代理组选择在服务启动后通过本机控制 API 重放；HTTP provider
缓存和地理数据库由应用重新下载。
归属记录只在与当前 XML 身份匹配时用于撤销原来的 DNS/TUN 改动；这里的
身份属于系统配置，不能识别物理设备。如果手动编辑备份字段造成校验不符，
已有有效本地配置仍可启动；停止服务后可在页面修复备份，或执行
`configctl mihomo repair-backup` 验证并修复校验值。
仍与内置预设等价的 Mihomo 合并配置按已安装版本的预设重建，其他合并
配置按原文恢复。

配置文件的内容、权限、空目录、文件缺失和 `enable="NO"` 状态都会保存。
已经同步过的旧镜像不会在普通服务重启时覆盖较新的外部修改。检测到系统
配置回退时，后台任务会阻止用旧运行文件覆盖刚恢复的 XML。
镜像写入还会在原生独占锁内检查刚读到的版本；读取后发生系统恢复时，
本次写入会失败并保留新恢复的配置。

共用组件在启动前执行 `reconcile`：XML 镜像版本未变时，只补回已经完全
消失且原镜像记录为存在的配置根目录或独立文件。其他仍存在的配置根保留
较新的外部修改，已有目录内被删除的文件也不会被旧镜像补回；原镜像记录
为缺失的根不会重新创建。XML 镜像版本改变或显式执行 `import-config` 时，
才完整还原镜像。只恢复缺失根时，无法在该范围内验证安全的跨根符号链接
会明确报错，可以改用显式完整还原。

Speedtest 也使用已应用版本标记，普通开机保留镜像失败后仍已保存的新设置；
设置文件完全消失时，即使标记未变，仍会从 XML 重新建立设置。

## 实现范围

六个新增文件镜像插件分别安装共用组件的副本，避免软件包之间争用相同
路径。PHP 组件先获取原生配置锁、重新加载配置，再只修改所属插件节点，
保留其他插件的数据。相同镜像不会重复产生配置历史。
这些备份模型不执行通用 MVC 模型迁移，避免重写镜像中的未来字段或属性；
备份内容的格式检查由各自的镜像脚本负责。

六个共用组件插件的镜像是压缩、Base64 编码并校验的文件集合。恢复先验证完整镜像和允许的
路径，再预备全部文件，替换失败时回滚。支持自定义配置路径和树内的相对
符号链接；拒绝越出配置范围的链接、系统关键路径及需要执行 shell 的
路径表达式。Sing-box 的相对依赖路径因服务工作目录不固定，会明确报错，
请使用绝对路径。日志、PID、锁、缓存和编辑器保护密钥不属于配置。

共用组件的单个文件上限为 16 MiB，新压缩镜像上限为 1 MiB，仍能读取
此前不超过 16 MiB 的压缩镜像。展开后的 JSON 上限为 32 MiB，最多遍历
4096 个条目（包括被排除的名称），绝对路径最多 63 个目录或文件名组成部分，
路径和符号链接目标均不超过 1023 字节。恢复较旧镜像时，允许当前插件新增
固定配置根和排除项；只恢复旧镜像实际拥有的范围，保留新增根及被新规则
排除的运行文件。动态配置根仍必须从镜像中的实际配置推导，并符合当前
路径保护规则。
共用组件的应用标记和自动同步时间保存在 `/conf/os-config-backup/<模块名>/`，
会迁移已有 `/var` 标记，不依赖可能使用内存盘的 `/var`。Speedtest 的应用
标记也在此目录；FRP 的持久标记为 `/usr/local/etc/frp/.backup-applied`。
这些标记只记录校验值或同步时间，不进入系统 XML 备份；新设备没有标记时
会按 XML 执行初次还原。
超限或无法读取的文件会使同步明确失败，保留之前的有效
备份。编码不提供加密；导出的配置包含账号凭据、订阅地址和私钥，可使用
系统备份加密选项保护它们。

Ttyd 的新备份软件包目前只支持 FreeBSD 15 amd64；旧版 FreeBSD 14 下载
继续保留，不能作为新版备份功能已在该目标验证的证明。

实机测试记录见 [CONFIG-BACKUP-TESTS.md](CONFIG-BACKUP-TESTS.md)。
