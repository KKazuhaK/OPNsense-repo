#!/bin/sh
# Firmware transitions can remove packages without setting PKG_UPGRADE.
# Preserve desired plugin registration, configuration and trust for restoration.
# The official Plugins page unregisters an explicit administrator removal.
exit 0
