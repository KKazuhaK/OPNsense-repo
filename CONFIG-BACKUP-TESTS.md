# 配置备份实机测试

日期：2026-09-13。设备：OPNsense 26.7.3_11，FreeBSD 15.1 amd64，
PHP 8.5，Python 3.13.15。测试通过现有 SSH 和 API 访问真实路由器。

本记录对应配置备份功能的首次实机验证。后续全部插件测试、回归中发现的修复
及更新后的原生测试结果见 [TESTING.md](TESTING.md)。下列包摘要保留为该轮验证记录。

## 系统 XML 导出与恢复

使用六个插件的真实镜像脚本和原生 `OPNsense\Core\Config` 对象，把测试
配置同步到设备当前 `/conf/config.xml`。随后通过系统原生
`GET /api/core/backup/download/this` 下载 XML，验证六个配置镜像都存在，
并逐字节解码核对内容。导出的 XML 为 62834 字节；大小只反映此轮测试设备
的配置，不是软件限制。

| 插件 | 验证恢复的文件数 | 验证内容 |
| --- | ---: | --- |
| Staticarp | 4 | 绑定、接口、设置和禁用状态 |
| Lucky | 3 | 自定义目录、账号凭据、令牌和私钥 |
| DDNS-Go | 3 | 默认及自定义配置、密码和提供商令牌 |
| EasyTier | 2 | 网络密钥和禁用状态 |
| Ttyd | 4 | 自定义命令、代理覆盖、证书和私钥 |
| Sing-box | 12 | 订阅 URL、代理密码、自定义配置、TLS/SSH 私钥、规则、hosts、信任目录和 ACME 账号 |

把文件改写并生成更新的镜像，再把下载 XML 中属于这六个插件的镜像恢复
到原生配置对象，验证后台同步拒绝覆盖待恢复配置。执行启动前的 reconcile
后，28 个文件内容和 `0600` 权限全部一致，禁用状态仍为 `NO`。
之后删除这些文件并执行显式 import-config，验证全部文件再次完整还原。

测试文件使用专用目录映射，内容包含实际凭据字段及完整测试值，不使用
编辑器掩码或占位标记。没有恢复整个生产路由器配置，也没有启动代理、VPN
或修改生产接口；恢复流程对插件文件的操作通过真实设备上的隔离目录验证。

## 后台同步与并发

在设备上运行 Lucky 和 DDNS-Go 的真实独立后台同步进程，直接改写其应用
配置文件，均约 1.39 秒后在原生 XML 中读到完整的新凭据。
应用程序是否运行不影响该后台同步。测试结束后进程均已退出。

上述时延属于首次验证时的写入器。当前写入器等待文件稳定约两秒，并将同一
插件的自动同步间隔限制为至少 30 秒；该间隔在后台进程重启后仍然有效。
首次测量不能作为当前写入器的固定响应时间，当前行为见 [CONFIG-BACKUP.md](CONFIG-BACKUP.md)。

同时运行六个原生 PHP/XML 写入任务，每个修改不同插件的 rc 测试配置。
完成后六个插件的配置节点全部保留，验证原生锁的重新加载和合并行为。

## Speedtest、FRP 和仓库清单

使用该轮源码再次通过真实 Core 配置对象保存 Speedtest 和 FRP 测试镜像，
下载系统 XML 后恢复到隔离文件。三个测速偏好、两份含 CRLF、Unicode 和
认证凭据的 TOML，以及两个禁用状态全部一致；删除文件后的重建、`0600`
权限和重复恢复也通过。此轮实际导出的 XML 为 59161 字节。

两种镜像改变已知字段及重复保存时，原生 XML 中的未来标量字段、嵌套字段
和备份节点属性仍保留；部分 FRP 镜像导入只返回实际存在的字段。原始两个
模块完整恢复，其他模块摘要不变，测试没有启动应用服务。

仓库清单的 47 项测试在设备原生运行时全部通过。额外使用真实 Core 的隔离
XML 验证未知字段和属性保留、相同内容不重复写入，以及 16 个并发写入者
保留其他八个插件节点。仓库软件包在设备构建并核对文件及安装脚本，未安装。

## Mihomo 与恢复竞争

Mihomo 使用该轮源码同步真实 Core 配置对象，再下载原生系统 XML 并恢复
隔离配置文件。订阅凭据、控制密钥、原始 YAML、本地 provider 文件及
`0640` 权限、服务禁用状态、代理选择和 DNS/TUN 归属记录均核对通过。
恢复后的透明代理和授权保持关闭；原始备份节点、其他 XML 节点和生产
Mihomo PID 均保持原值，完整系统导出及隔离测试目录已删除。

特意在镜像读取与写入之间插入系统配置恢复，验证共用组件、Mihomo、FRP
和 Speedtest 都在原生独占锁内拒绝旧版本写入，保留刚恢复的 XML，不产生
配置历史；共用组件和 Mihomo 的应用标记保持原值。校验覆盖原节点缺失、已有节点及包含 Unicode、
emoji、斜线、DEL 和 CRLF 的字段。

Mihomo 的 DNS/TUN 恢复辅助脚本另用真实 Core 的隔离 XML 验证较新的字段
和属性保留、仅清理所属接口及规则、重复执行不保存，以及 16 个并发写入
任务保留其他八个插件的备份节点。

## 软件包、启动与失败路径

- 六个插件在设备上构建原生软件包，检查包含镜像模型、Python/PHP 组件、
  启动前恢复钩子和独立后台服务；解码软件包脚本并与源码核对。
- FRP 和 Speedtest 该轮安装包的 43 个运行文件及七个原生解码脚本与该轮
  源码一致；FRP 107 项原生测试及独立字段传输测试通过。
- 使用真实 FreeBSD rc.subr 和 daemon 的隔离测试，验证后台进程启动、
  重复启动、停止，以及服务重启时先停止写入者、再恢复并重新读取 rc。
- 在原生运行时验证文件缺失与无镜像的区别、权限、禁用状态、自定义路径、
  shell 转义的字面路径、损坏镜像、越界路径和符号链接拒绝。
- 注入文件替换失败，验证已改写的文件回滚；模拟传输失败，验证保留原有
  有效镜像并向用户返回错误或警告。
- 原有 MVC 后端和仓库测试继续通过，相关测试加入 CI。

该轮本地回归共执行 531 项，其中 34 项因本地缺少设备原生依赖而跳过；
对应项目已在设备运行通过，包括 Mihomo 173 项、FRP 107 项和仓库 47 项
原生测试。源码检查通过：45 个 XML、70 个 Python 文件、83 个 shell
脚本，以及设备 PHP 8.5 对 97 个 PHP/INC 文件的语法检查。

原生专用的隔离 Core 检查命令包括：

```sh
php src/os-sing-box/tests/native/test-config-setup.php
php src/os-kazuha-repo/tests/native/test-manifest-lock.php
php src/os-mihomo/tests/native/test-integration-lock.php
php src/common/tests/native/test-backup-cas.php
php src/common/tests/native/test-backup-model.php
php src/os-speedtest/tests/native/test-backup-cas.php
php src/os-mihomo/tests/native/test-backup-fields.php
php src/os-frp/tests/native/test-backup-fields.php
```

前六项需要设备已安装的 OPNsense Core；后两项使用私有 XML 和模拟 Core
字段模型，另有前述真实系统 XML 导出验证。所有命令都使用隔离测试目录。

当前补充的 `test-backup-model.php` 使用真实 Core/BaseModel 和私有 XML，
验证六个归档模型的迁移不创建缺失节点，也不改写未来字段和属性。Mihomo
VNET 生命周期 harness 同样使用真实 Core 和测试生成的私有 XML；其
configd、服务、PF、模板和配置修订适配器仍为模拟实现，详见
[原生测试说明](src/os-mihomo/tests/native/README.md)。本记录的首次验证
计数和包摘要不代表当前完整测试集合，最新回归记录见 [TESTING.md](TESTING.md)。

## 当前补测记录

同日的专用 OPNsense 测试机回归及受影响套件补测覆盖当前 802 项 Python
检查，799 通过、3 项条件跳过；17 个独立 PHP/Core/API 检查和 15 个 Volt
视图检查通过。最终 14 个候选软件包全部通过源码逐字节验证。Mihomo 的
真实 Core VNET 生命周期运行 17 项全部通过，SIGKILL 后的恢复耗时约
5.736 秒；报告保留模拟 PF、模板、服务和配置修订适配器的边界。

VNET 重装验证保留私有 XML 备份、擦除应用配置后的行政 Stop 恢复；只有
精确移除私有 XML 的 Mihomo 备份节点并再次擦除应用存储，才验证无备份
首次安装时的默认代理端口启动。其他插件、系统及 cron 节点保持在 XML 中。

FRP 与 Speedtest 清除自身测试 XML 节点后实际安装，确认安装自动建立
配置镜像；再移走应用配置存储并重装，FRP TOML 摘要、Speedtest JSON
内容和 FRP 禁用状态 `NO` 完全一致。通过系统原生 API 导出的
50751 字节 XML 核对四个插件模块与仓库清单，大小只代表本轮测试配置。
这些部署与重装仅发生在专用测试机，未部署至生产。

共享组件的原生检查覆盖 `/conf` 中持久标记、擦除 `/var` 后恢复、仅重建
完全缺失的归档根并保留其他根的新修改，以及后台进程重启后的 30 秒同步
间隔。实际 watcher 测试按该预算等待 35 秒，验证状态和端口改动捕获、
重复启动拒绝与父子进程停止。真实 Core 的 CAS 与归档模型检查同时通过。
本地受影响的可移植 PHP 套件已复测通过；完整发布入口会再次运行本地测试。

## 清理

测试前单独保存六个插件原来的备份节点。结束后使用原生配置锁恢复这些
节点，保留其他软件包和系统的并发变更；测试运行文件、同步进程和临时
目录已清理。插件构建产物保留在隔离构建目录，未部署至生产服务。
Mihomo、Speedtest 和 FRP 测试涉及的原节点也分别精确恢复；本地生成的
完整系统 XML 导出已删除，仅保留不含生产配置内容的测试报告。
系统配置历史中可能保留本轮测试产生的历史快照，其中包含隔离测试数据。
